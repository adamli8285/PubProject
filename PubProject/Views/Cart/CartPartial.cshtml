@model PubProject.Models.ViewModels.Cart.CartVM

<div class="ajaxcart" style="color:white;">
     @Model.Quantity item(s) in your shopping cart - $@Model.Price
    <br />
    @Html.ActionLink("Checkout", "Index")
</div>